apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: model-service-claim0
  name: model-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
